# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# AndroidX package structure
android.useAndroidX=true
# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=true

# Architectures to build for
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# New Architecture (disabled for this project)
newArchEnabled=false

# Hermes JS engine
hermesEnabled=true

# 16 KB page size support (Android 15+)
# Locally compiled native code will use flexible page sizes.
# Pre-built RN 0.73 .so files still need useLegacyPackaging (set in app/build.gradle).
android.experiments.cacheCompileLibResources=true
